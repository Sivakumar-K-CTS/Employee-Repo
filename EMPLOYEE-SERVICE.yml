# server:
#   port: 8081

eureka:
  client:
    service-url:
      default-zone: http://EUREKA-SERVER
      
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/EmployeeRealm
  
      


